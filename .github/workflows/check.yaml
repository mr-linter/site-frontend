name: Check

on:
  pull_request:
    branches: [ master ]

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: print
        run: "echo ${{ github.event.pull_request.user.login }} ${{ github.event.pull_request.title }}"
      - name: 'Auto Merge'
        run: 'echo "kuku"'
        if: github.event.pull_request.user.login == 'artarts36' && startsWith(github.event.pull_request.title, '[AUTO]')
