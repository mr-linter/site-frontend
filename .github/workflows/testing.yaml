name: Check

on:
  push:
    branches: [ check-if ]

jobs:
  testing:
    runs-on: ubuntu-latest

    steps:

    - name: Failing step
      id: demo
      run: exit 1

    - name: The demo step has failed
      if: ${{ failure() }}
      run: "echo 'kuku'"
